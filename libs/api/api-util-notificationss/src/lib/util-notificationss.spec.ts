import { utilNotificationss } from './util-notificationss';

describe('utilNotificationss', () => {
  it('should work', () => {
    expect(utilNotificationss()).toEqual('util-notificationss');
  });
});
